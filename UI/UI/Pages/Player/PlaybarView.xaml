<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SINTEF.AutoActive.UI.Pages.Player.PlaybarView">
    <ContentView.Content>
        <Grid x:Name="ContentGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition x:Name="RowDataPreview" Height="0" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Label x:Name="LabelTimeFrom" Text="0"
                   Grid.Row="1" Grid.Column="0"
            />
            <Slider Minimum="0" Maximum="10000" ValueChanged="Slider_ValueChanged"
                    Grid.Row="1" Grid.Column="1"
            />
            <Label x:Name="LabelTimeTo" Text="0"
                   Grid.Row="1" Grid.Column="2"
            />
        </Grid>
    </ContentView.Content>
</ContentView>